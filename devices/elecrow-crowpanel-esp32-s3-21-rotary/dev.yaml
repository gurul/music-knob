substitutions:
  name: "music-knob-dev"
  friendly_name: "Music Knob Dev"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  music_dashboard: !include packages.yaml

external_components:
  - source:
      type: local
      path: ../../components
    components: [artwork_image, mipi_rgb, speaker_group_helpers]
